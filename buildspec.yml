version: 0.2

phases:
  pre_build:
    commands:
      - echo install packages...
      - yarn install
  build:
    commands:
      - echo Create build...
      - yarn build
  post_build:
    commands:
      - echo Removing node modules folder
      - rm -rf node_modules/
artifacts:
  files:
    - '**/*'
